---
id: q-28617
title: Design a global load-balancing fabric for a real-time payments platform processing 1B transactions daily, with per-transaction idempotency, geo-failover, and edge-rate limiting. Propose an edge proxy using Nginx and a regional core using HAProxy, TLS termination at the edge, mTLS to backends, dynamic config via Consul/etcd, and graceful draining during rolling updates. Outline how you would implement traffic shaping, health checks, and DDoS resilience while meeting strict SLAs?
slug: design-a-global-load-balancing-fabric-for-a-real-time-paymen
channel: system-design
difficulty: advanced
tags: ["system-design","advanced"]
createdAt: 2026-04-20T16:41:22.436Z
readingTime: 10
excerpt: Design a global load-balancing fabric for a real-time payments platform processing 1B transactions daily, with per-transaction idempotency, geo-failov
---

> **Understanding Design a global load-balancing fabric for a real-time payments platform processing 1B transactions daily, with per-transaction idempotency, geo-failover, and edge-rate limiting. Propose an edge proxy using Nginx and a regional core using HAProxy, TLS termination at the edge, mTLS to backends, dynamic config via Consul/etcd, and graceful draining during rolling updates. Outline how you would implement traffic shaping, health checks, and DDoS resilience while meeting strict SLAs?**

## Overview

Design a global load-balancing fabric for a real-time payments platform processing 1B transactions daily, with per-transaction idempotency, geo-failover, and edge-rate limiting. Use Nginx at the edge with HAProxy as the regional core, implementing TLS termination at the edge and mTLS to backends. Leverage dynamic configuration via Consul/etcd with graceful draining during rolling updates to ensure zero-downtime deployments.

## Technical Details

## Implementation

Design a global load-balancing fabric for a real-time payments platform processing 1B transactions daily, with per-transaction idempotency, geo-failover, and edge-rate limiting. Use Nginx at the edge with HAProxy as the regional core, implementing TLS termination at the edge and mTLS to backends. Leverage dynamic configuration via Consul/etcd with graceful draining during rolling updates to ensure zero-downtime deployments.

## Architecture

Scalable design patterns and best practices.

## Real-World Examples

### Example: SYSTEM-DESIGN Implementation

Leading companies implement this pattern for production systems.

## Best Practices

- Follow SOLID principles
- Implement proper error handling
- Optimize for performance
- Ensure security

## Conclusion

Key insights and recommendations for implementation.

## References

- Official system-design documentation
- Engineering blogs
- Technical case studies
