---
id: q-20047
title: Design a global per-tenant API rate limiter using Redis as hot cache, MongoDB for tenant config, Cassandra for event logs, and DynamoDB for daily summaries. Implement a token-bucket rate limiter with explicit key schemas: Redis: rate:<tenantId> with fields tokens,last_refill; DynamoDB: dailyQuota; MongoDB: burst/refill rate; Cassandra: call logs. Provide concrete write paths, TTLs, and cross-store coordination and idempotent retry strategy to avoid double charging on retries?
slug: design-a-global-per-tenant-api-rate-limiter-using-redis-as-h
channel: database
difficulty: advanced
tags: ["database","advanced"]
createdAt: 2026-04-20T16:41:22.434Z
readingTime: 10
excerpt: Design a global per-tenant API rate limiter using Redis as hot cache, MongoDB for tenant config, Cassandra for event logs, and DynamoDB for daily summ
---

> **Understanding Design a global per-tenant API rate limiter using Redis as hot cache, MongoDB for tenant config, Cassandra for event logs, and DynamoDB for daily summaries. Implement a token-bucket rate limiter with explicit key schemas: Redis: rate:<tenantId> with fields tokens,last_refill; DynamoDB: dailyQuota; MongoDB: burst/refill rate; Cassandra: call logs. Provide concrete write paths, TTLs, and cross-store coordination and idempotent retry strategy to avoid double charging on retries?**

## Overview

Design a global per-tenant API rate limiter using Redis as the hot cache, MongoDB for tenant configuration, Cassandra for event logs, and DynamoDB for daily summaries. Implement a token-bucket rate limiter with explicit key schemas:

- **Redis**: `rate:<tenantId>` with fields `tokens` and `last_refill`
- **DynamoDB**: `dailyQuota`
- **MongoDB**: `burst` and `refill_rate`
- **Cassandra**: `call_logs`

Describe the concrete write paths, TTLs, and cross-store coordination. Additionally, explain an idempotent retry strategy to avoid double charging on retries.

## Technical Details

## Implementation

Design a global per-tenant API rate limiter using Redis as the hot cache, MongoDB for tenant configuration, Cassandra for event logs, and DynamoDB for daily summaries. Implement a token-bucket rate limiter with explicit key schemas:

- **Redis**: `rate:<tenantId>` with fields `tokens` and `last_refill`
- **DynamoDB**: `dailyQuota`
- **MongoDB**: `burst` and `refill_rate`
- **Cassandra**: `call_logs`

Describe the concrete write paths, TTLs, and cross-store coordination. Additionally, explain an idempotent retry strategy to avoid double charging on retries.

## Architecture

Scalable design patterns and best practices.

## Real-World Examples

### Example: DATABASE Implementation

Leading companies implement this pattern for production systems.

## Best Practices

- Follow SOLID principles
- Implement proper error handling
- Optimize for performance
- Ensure security

## Conclusion

Key insights and recommendations for implementation.

## References

- Official database documentation
- Engineering blogs
- Technical case studies
