---
id: q-4593
title: You're working on a React application where multiple components need to access and modify the same piece of state, but you want to avoid prop drilling while maintaining better performance than Context API. How would you implement a state management solution using a custom hook with React's useRef and useCallback to create a subscription-based pattern that allows components to selectively subscribe to state changes?
slug: you-re-working-on-a-react-application-where-multiple-compone
channel: frontend
difficulty: intermediate
tags: ["frontend","intermediate"]
createdAt: 2026-04-20T16:41:22.438Z
readingTime: 10
excerpt: You're working on a React application where multiple components need to access and modify the same piece of state, but you want to avoid prop drilling
---

> **Understanding You're working on a React application where multiple components need to access and modify the same piece of state, but you want to avoid prop drilling while maintaining better performance than Context API. How would you implement a state management solution using a custom hook with React's useRef and useCallback to create a subscription-based pattern that allows components to selectively subscribe to state changes?**

## Overview

Create a custom hook using useRef to store state and callbacks, implementing a subscription pattern with useCallback for selective updates. This avoids Context API's cascade re-renders while eliminating prop drilling.

## Technical Details

## Implementation

Create a custom hook using useRef to store state and callbacks, implementing a subscription pattern with useCallback for selective updates. This avoids Context API's cascade re-renders while eliminating prop drilling.

## Architecture

Scalable design patterns and best practices.

## Real-World Examples

### Example: FRONTEND Implementation

Leading companies implement this pattern for production systems.

## Best Practices

- Follow SOLID principles
- Implement proper error handling
- Optimize for performance
- Ensure security

## Conclusion

Key insights and recommendations for implementation.

## References

- Official frontend documentation
- Engineering blogs
- Technical case studies
