[{
  "id": "cissp-advanced-bell-lapadula-biba-1751846400000",
  "question": "A regulated electronic trading system enforces Bell-LaPadula mandatory access control at the filesystem level. A process running at SECRET clearance creates a new output file, which inherits the SECRET label by default. Before completing, the process relabels the output file to UNCLASSIFIED so downstream consumers can read it. Identify the specific BLP property this label-change operation bypasses, explain why label reclassification defeats mandatory access control entirely, and describe the kernel-level mechanism SELinux uses to prevent this attack?",
  "answer": "The star property (no write down) is bypassed. BLP checks the object label at write-time, not the transition history. Relabeling from SECRET to UNCLASSIFIED before writing means the check passes against the new label. SELinux prevents this with mandatory type enforcement: the SECRET-domain process needs an explicit allow rule to transition the file type via the privileged security_server syscall that validates the full MLS range change, with every attempt audited.",
  "explanation": "## How It Works\n\nBell-LaPadula's **star property** (*-property) prohibits a subject from writing to an object at a lower classification level. The check evaluates the subject's clearance against the object's current security label at the instant of the write() system call. If the file label says UNCLASSIFIED and the subject is at SECRET, the write is permitted — writing to an equal level, not down.\n\nThe attack exploits this by **reclassifying the object before the write**. The sequence is:\n\n1. Create file at SECRET label (inherited from process, no violation)\n2. Write SECRET content to file (write to same level, no violation)\n3. Relabel file to UNCLASSIFIED (label-management operation — BLP has no rule for who can change labels)\n4. Write SECRET content to file (check passes: file is now UNCLASSIFIED, subject is SECRET)\n\nStep 3 is the critical gap. BLP was designed to govern **information flow through read and write operations**, not to govern the security of the labeling mechanism itself. The model assumes labels are part of the trusted computing base, managed by an administrator outside the model's scope. It has no property constraining label mutation by the subjects subject to those labels.\n\n## Code Example\n\n```python\nimport os\n\nclass TradingFile:\n    def __init__(self):\n        self.labels = {}\n\n    def create(self, path, content, clearance):\n        fd = os.open(path, os.O_CREAT | os.O_WRONLY)\n        self.labels[path] = clearance\n        os.write(fd, content.encode())\n        os.close(fd)\n\n    def relabel(self, path, new_label):\n        # BLP has no rule preventing this — label management\n        # is outside the model. This is the bypass.\n        self.labels[path] = new_label\n\n    def write(self, path, content, subject_label):\n        obj_label = self.labels[path]\n        # BLP star-property: write allowed if obj >= subj\n        # NOTE: checks CURRENT label, not label history\n        if obj_label >= subject_label:\n            fd = os.open(path, os.O_WRONLY)\n            os.write(fd, content.encode())\n            os.close(fd)\n        else:\n            raise PermissionError(\"BLP *-property violation\")\n\n# This succeeds despite SECRET->UNCLASSIFIED data flow\nmgmt = TradingFile()\nmgmt.create(\"/tmp/trade\", \"position_data\", \"SECRET\")\nmgmt.relabel(\"/tmp/trade\", \"UNCLASSIFIED\")  # bypass\nmgmt.write(\"/tmp/trade\", \"more_data\", \"SECRET\")  # PASS\n```\n\nWithout SELinux, this program succeeds. The SELinux prevention:\n\n```bash\n# The SECRET domain must have explicit permission to relabel:\nallow secrat_domain unclassified_file_type:file relabelto;\n# Without this rule, lsetfilecon returns -EPERM\n# All attempts logged to audit.log:\ntype=AVC msg=audit(1718000000.000:1):\n  avc:  denied  { relabelto } for  pid=1234\n  comm=\"trading\" scontext=secrat_domain\n  tcontext=unclassified_file_type tclass=file\n```\n\n## Key Points\n\n- **BLP checks labels, not history**: the write-time check evaluates only the current label. Prior label states are invisible to the reference monitor.\n- **Label management is outside the BLP model**: BLP constrains read and write operations, not the labeling infrastructure. This is the fundamental blind spot.\n- **SELinux closes it through type enforcement**: the relabelto permission chains the label transition to the process domain, creating an auditable policy decision.\n- **Audit logging is non-bypassable**: every relabel attempt — denied or granted — reaches the audit subsystem, which is write-once for integrity.\n\n## Common Mistake\n\nDevelopers assume that BLP's mandatory access control covers every way confidentiality can be violated. The most frequent error is believing that once an object receives a label, that label is immutable by the subject operating on it. BLP has no **label-integrity property** — nothing in the model prevents a subject from changing an object's label before writing to it. Production systems that implement only BLP without a separate label-management policy are trivially bypassable by any process with privilege to change security attributes on files it owns.\n\n## Interview Tip\n\nInterviewers are testing whether you understand that **MAC models govern operations, not the labeling infrastructure**. A strong answer recognizes that BLP's blind spot is label transitions being outside the model, names the exact SELinux permission (relabelto), and explains that the security server validates the MLS range change against both the process clearance and the file's current context before allowing the transition. A weaker answer correctly states BLP properties but fails to identify label mutation as the bypass mechanism or omits the specific kernel mechanism that closes it.",
  "diagram": "flowchart TD\n  P[SECRET Process] --> C[Create File\nLabel = SECRET\ninherited from process]\n  C --> W1[Write SECRET data\nBLP: write to same level\nPASS]\n  W1 --> RL[Relabel to UNCLASSIFIED\nBLP: NO RULE GOVERNING\nLABEL TRANSITIONS\nBYPASS]\n  RL --> W2[Write more SECRET data\nBLP: file label is now\nUNCLASSIFIED\nwrite to same level\nPASS]\n  W2 --> RD[UNCLASSIFIED Consumer\nReads file\nBLP: read down\nPASS\nDATA LEAKED]\n  RL -.-> GAP[GAP: BLP has no\nlabel-integrity property]\n\n  subgraph MITIGATION[SELinux Mitigation]\n    P2[SECRET Process] --> SC[invokes security_server\nvia lsetfilecon syscall]\n    SC --> DENIED{relabelto permission\nin policy?}\n    DENIED -->|No - EPERM| LOG[Logged to audit.log]\n    DENIED -->|Yes - OK| MLS[MLS range validated\nagainst process clearance\nand file context]\n    MLS --> LOG\n  end",
  "difficulty": "advanced",
  "tags": ["cissp", "access-control", "bell-lapadula", "selinux", "mandatory-access-control", "label-integrity", "trusted-subject", "kernel-security"],
  "channel": "cissp-beginner-bell-lapadula-biba",
  "subChannel": "general",
  "sourceUrl": null,
  "videos": {
    "shortVideo": null,
    "longVideo": null
  },
  "companies": [
    "Citadel",
    "Airbnb",
    "PayPal"
  ],
  "eli5": "A guard checks your badge every time you enter a room with classified files. But the guard doesn't check what label you put on the file cabinet — only what files you take out. You can empty a SECRET cabinet, re-label it UNCLASSIFIED, and walk out. SELinux is a second guard who watches the labels themselves.",
  "voiceKeywords": null,
  "voiceSuitable": 1,
  "isNew": 1,
  "lastUpdated": "2026-07-28T00:00:00.000Z",
  "createdAt": "2026-07-28T00:00:00.000Z"
}]
