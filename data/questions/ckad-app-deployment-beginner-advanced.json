[
  {
    "question": "A Deployment runs 6 replicas with strategy.type: RollingUpdate, maxSurge: 1, and maxUnavailable: 0. After updating the container image, the new ReplicaSet creates pods that never become Ready because their readiness probes fail. Walk through the exact state of both ReplicaSets, the available application capacity during this state, the specific kubectl commands to diagnose the failure, and the resolution. How does the behavior differ if maxUnavailable were changed to 1?",
    "answer": "With maxSurge=1 and maxUnavailable=0, the controller creates 1 new pod but cannot terminate old pods until new pods are Ready. The new pod fails its readiness probe. 7 pods exist but only 6 serve traffic. kubectl rollout status hangs. kubectl describe deployment shows old RS at 6/6 ready and new at 0/1 ready. kubectl describe pod reveals the failing probe. Resolution: kubectl rollout undo. With maxUnavailable=1, the controller terminates one old pod, reducing capacity from 6 to 5.",
    "explanation": "## How It Works\n\nThe Deployment controller reconciles desired state through ReplicaSet management. When the pod template changes (e.g., container image), the controller creates a new ReplicaSet and begins a rolling update.\n\nThe rolling update algorithm works in a loop:\n1. Scale up the new ReplicaSet by min(maxSurge, desiredReplicas - newReadyReplicas)\n2. Scale down the old ReplicaSet by min(maxUnavailable, ...)\n3. Repeat until all pods are on the new ReplicaSet\n\n**maxSurge** controls how many extra pods (beyond the desired replica count) can be created. **maxUnavailable** controls how many pods can be unavailable during the update. These parameters gate every scaling decision.\n\nWith maxSurge=1 and maxUnavailable=0:\n- The controller can create 1 pod above the desired 6 (total 7)\n- The controller cannot terminate any old pod because maxUnavailable=0 means zero pods may be unavailable\n- The new pod must become Ready before any old pod is terminated\n- If the new pod never becomes Ready, the rollout stalls permanently\n\nThe Service continues routing traffic to the old ReplicaSet's pods because they remain Running and Ready. The new pod is excluded from the Service endpoint because its readiness probe fails.\n\n## Code Example\n\n```yaml\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: web-app\nspec:\n  replicas: 6\n  strategy:\n    type: RollingUpdate\n    rollingUpdate:\n      maxSurge: 1\n      maxUnavailable: 0\n  selector:\n    matchLabels:\n      app: web-app\n  template:\n    metadata:\n      labels:\n        app: web-app\n    spec:\n      containers:\n      - name: app\n        image: registry.example/web-app:v2\n        readinessProbe:\n          httpGet:\n            path: /healthz\n            port: 8080\n          initialDelaySeconds: 5\n          periodSeconds: 5\n```\n\nDiagnosis commands:\n\n```bash\n# Check rollout status (will hang indefinitely)\nkubectl rollout status deployment/web-app\n\n# Inspect both ReplicaSets\nkubectl describe deployment/web-app\n# Output shows:\n#   OldReplicaSet: 6/6 ready\n#   NewReplicaSet: 0/1 ready (1 new pod stuck)\n\n# Check the stuck pod\nkubectl get pods -l app=web-app\n# One pod shows 0/1 Ready with status Running\n\n# Find the readiness probe failure\nkubectl describe pod web-app-<new-hash>-<id>\n# Events show: Readiness probe failed: HTTP probe failed with statuscode: 503\n\n# Check pod logs for root cause\nkubectl logs web-app-<new-hash>-<id>\n```\n\nResolution:\n\n```bash\n# Rollback to previous revision\nkubectl rollout undo deployment/web-app\n\n# Verify rollback completes\nkubectl rollout status deployment/web-app\n```\n\n## Key Points\n\n- **maxUnavailable=0 guarantees full capacity during rollout but creates a hard dependency on readiness probes**\n- **maxSurge=1 limits resource overhead to 1 extra pod during the update**\n- **Readiness probe failures block the rollout entirely when maxUnavailable=0 because old pods cannot be terminated**\n- **The Service still works correctly during the stall since it only targets Ready pods from the old ReplicaSet**\n- **Setting maxUnavailable=1 allows the rollout to progress by sacrificing one replica of capacity**\n\n## Common Mistake\n\nSetting maxUnavailable=0 without a readiness probe, or with a misconfigured readiness probe that never passes. The rollout stalls silently: no errors are emitted, no crashes occur, but the new version never rolls out. Operators often wait minutes before checking `kubectl rollout status` and finding it stuck. The underlying issue is that maxUnavailable=0 makes readiness probe correctness a correctness requirement for the entire update process. If the probe is too strict, has an incorrect path, or the new image has a bug, the entire deployment pipeline blocks. This is why production deployments typically use a small nonzero maxUnavailable (like 1 or 10%) combined with a PodDisruptionBudget for finer control.\n\n## Interview Tip\n\nThe interviewer is testing whether you understand the Deployment controller as a state machine, not as a simple pod-replacement tool. A good answer explains the two-ReplicaSet reconciliation loop. A great answer explains how maxSurge and maxUnavailable act as mutual constraints that govern every scaling decision, that readiness probes gate the transition between ReplicaSets when maxUnavailable=0, and how this creates a tension between availability guarantees and deployment velocity. Mentioning that PodDisruptionBudget operates independently from the Deployment's rolling update strategy and can cause additional stalls during voluntary disruptions like node drains demonstrates deeper architectural knowledge.",
    "diagram": "flowchart TD\n  A[Deployment: 6 replicas] --> B[Update image to v2]\n  B --> C[Create New ReplicaSet: scale to 1]\n  C --> D{New pod Ready?}\n  D -- No --> E[Old RS: 6/6 ready, New RS: 0/1 ready]\n  E --> F[Rollout stalled at 7 total pods]\n  D -- Yes --> G[Scale down Old RS by maxUnavailable]\n  G --> H{Old RS at 0 replicas?}\n  H -- No --> C\n  H -- Yes --> I[Deployment complete]\n  F --> J[kubectl rollout undo]\n  J --> K[Scale New RS to 0, restore Old RS]\n  K --> I\n\n  style D stroke:#f00,stroke-width:2px\n  style F stroke:#f00,stroke-width:2px",
    "companies": [
      "Airbnb",
      "Hugging Face",
      "LinkedIn"
    ],
    "sourceUrl": null,
    "videos": {
      "shortVideo": null,
      "longVideo": null
    }
  }
]
