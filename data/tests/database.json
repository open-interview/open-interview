[
  {
    "id": "test-database",
    "channelId": "database",
    "channelName": "Database",
    "title": "Database Knowledge Test",
    "description": "Test your understanding of Database with 25 questions.",
    "questions": [
      {
        "id": "database-q-0",
        "questionId": "q-1160",
        "question": "How would you implement and maintain an indexing strategy for a database that experiences frequent schema changes and evolving query patterns, while ensuring minimal performance degradation during migrations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a composite B-tree index on (user_id, created_at DESC) to fetch a user’s orders by date, a hash index on (status) for fast equality lookups, and an optional B-tree on (created_at) for daily re",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use partial indexes, automated index monitoring, and gradual migration strategies with feature flags to handle schema changes while maintaining query performance.",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "EXPLAIN ANALYZE should show partition pruning on event_date, with min/max stats used to prune partitions. Use predicates on the partition key, not wrapped in functions: event_date >= '2026-01-01' AND",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a composite B-tree index on (region, user_id, created_at DESC) to efficiently support region-time range queries. This column ordering allows the database to filter by region first, then by user within that region…",
            "isCorrect": false
          }
        ],
        "explanation": "Implementing a robust indexing strategy for databases with frequent schema changes requires a multi-layered approach. First, establish automated index monitoring that tracks query performance, index usage statistics, and identifies unused or redundant indexes. This monitoring should trigger alerts when query patterns shift significantly, allowing proactive index adjustments before performance degrades. Use database-specific tools like PostgreSQL's pg_stat_user_indexes or MySQL's performance_schema to gather real-time metrics.\n\nSecond, adopt a gradual migration strategy using partial indexes and feature flags. When adding new columns or changing existing ones, create new indexes alongside existing ones rather than dropping old ones immediately. Implement partial indexes that cover only the most frequently queried data subsets, reducing storage overhead and maintenance costs. Use application-level feature flags to gradually route queries to use new indexes, allowing you to validate performance improvements before fully committing to the new schema.\n\nThird, implement automated index lifecycle management with scheduled maintenance windows. Create scripts that can safely drop unused indexes after a grace period, rebuild fragmented indexes during low-traffic periods, and generate recommendations for new indexes based on query analysis. This approach should include rollback capabilities and performance baselines to ensure changes don't negatively impact production workloads. Consider using database-agnostic tools like Liquibase or Flyway for schema versioning combined with custom index management scripts.",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-1",
        "questionId": "q-28063",
        "question": "Given a normalized Postgres schema: customers(id PK, region, created_at), orders(id PK, customer_id FK, order_date, status), order_items(id PK, order_id FK, product_id FK, quantity, unit_price), products(id PK, category, name, price). For region 'EMEA' and 2025-01-01..2025-12-31, compute total_revenue by category and top 10 products by revenue. Provide the SQL, then propose indexing (composite/partial/covering) and discuss normalization trade-offs here?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use SERIALIZABLE for the risk path to prevent write skew and phantom reads. Lock the involved rows with SELECT FOR UPDATE in the transfer path; if SERIALIZABLE hurts throughput, use FOR UPDATE on affe",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a composite B-tree index on (region_code, created_at DESC, user_id) for region-scoped, time-bounded queries and user lookups within a region. Add a separate hash index on (user_id) for efficient global per-user look…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a single SQL with three CTEs: recent as completed orders in last 90 days, per_user as SUM(qty*price) by user in region, and per_user_cat as SUM(qty*price) by user, region, category. Then apply ROW",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "SQL plan (category and top products): SELECT pr.category AS category, SUM(oi.quantity*oi.unit_price) AS revenue FROM orders ord JOIN order_items oi ON oi.order_id = ord.id JOIN products pr ON oi.produ",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests multi-join SQL, aggregation, and practical index design in Postgres; emphasizes 3NF vs denormalization trade-offs.\n\n## Key Concepts\n\n- Multi-table joins and GROUP BY\n- Composite and partial indexes (e.g., on (region, order_date)) and covering indexes\n- Normalization trade-offs: 3NF vs denormalized star schema\n\n## Code Example\n\n```sql\n-- Example skeletons shown in answer above\n```\n\n## Follow-up Questions\n\n- How would you measure index effectiveness in EXPLAIN ANALYZE?\n- How would data skew affect the top-10 query and how would you mitigate it?",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-2",
        "questionId": "q-9588",
        "question": "You operate a globally scaled event-tracking DB. Access patterns include: 1) range queries on event_time within region and device_type, 2) exact lookups by user_id and event_id, 3) high-frequency top-N by region and product_id for the last hour. Design a hybrid indexing strategy using B-tree, hash, and composite indexes to satisfy these patterns. Provide concrete CREATE INDEX statements, discuss maintenance, partitioning, and how the planner will choose plans under write-heavy load?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement an idempotent, versioned cross-store write path: 1) Write to DynamoDB using a composite key (campaign_id:user_id:version) with the new channel preference and version number; 2) Update Redis per-user current_ch…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Diagnose whether pruning uses the partition key directly in the WHERE clause. Run EXPLAIN ANALYZE with filters: where event_day >= '2026-02-01' and event_day < '2026-02-08' and event_type='login'. If",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a hybrid plan: 1) B-tree composite index on (region, device_type, event_time DESC) to support range queries; 2) hash index on (user_id, event_id) for exact lookups; 3) composite index on (regi",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Begin a transaction; SELECT balance FROM accounts WHERE id = :from FOR UPDATE; SELECT balance FROM accounts WHERE id = :to FOR UPDATE; IF from_balance < :amount THEN RAISE; UPDATE accounts SET balance",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses ability to design a mixed indexing strategy across B-tree, hash, and composite indexes for a global, write-heavy workload with partitioning considerations.\n\n## Key Concepts\n- Hybrid indexing: B-tree, hash, composite\n- Partitioning by region/time for pruning\n- Index maintenance and statistics updates\n- Query planner plan selection and index-only scans\n\n## Code Example\n```sql\n-- B-tree composite for range scans by region and device_type\nCREATE INDEX events_region_devtime ON events (region, device_type, event_time DESC);\n\n-- Hash index for exact lookups on user/order\nCREATE INDEX events_user_event_hash ON events USING hash (user_id, event_id);\n\n-- Composite index to support top-N by region/product_time\nCREATE INDEX events_region_prod_time ON events (region, product_id, event_time DESC);\n```\n\n## Follow-up Questions\n- How would you test index usefulness under mixed read/write load?\n- How would you handle partition pruning if a region becomes hot?",
        "difficulty": "advanced"
      },
      {
        "id": "database-q-3",
        "questionId": "q-13370",
        "question": "Design a cross-store write path for a fintech fraud alert pipeline using Redis (recent alerts cache), MongoDB (customer profiles), Cassandra (transaction logs), and DynamoDB (immutable audit logs). When a transaction is flagged as suspicious, specify concrete keys, TTLs, an idempotent write workflow, cross-store conflict handling, and a read path to reconstruct the current alert with full context?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "- Use a single ACID transaction that debits A and credits B atomically. - Represent money in integer cents, and lock rows with FOR UPDATE to avoid race conditions. - Example (PostgreSQL): ``` BEGIN; UPDATE accounts SET…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Index orders(user_id, created_at DESC); consider including id and status in a covering index. To avoid N+1 queries, use a LATERAL join or DISTINCT ON. Example: SELECT u.region, u.id AS user_id, o.id AS latest_order_id,…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a single idempotent request_id to drive the workflow: Redis: SET alert:{req_id} with payload, TTL 3600; MongoDB: upsert customer profile to include alert ref; Cassandra: append per-customer transa",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Guarantee price correctness under concurrency using an MVCC-friendly approach with either SERIALIZABLE isolation or optimistic locking with versioning. SQL transaction flow: BEGIN; SET TRANSACTION ISOLATION LEVEL SERIAL…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests cross-store consistency, idempotency, and failure handling in a multi-datastore fraud workflow. Emphasizes concrete keys, TTLs, and read-back guarantees.\n\n## Key Concepts\n- Idempotent writes across Redis, MongoDB, Cassandra, DynamoDB\n- TTL management for cache freshness\n- Cross-store conflict resolution and compensating actions\n- Read-path reconstruction from multiple sources\n\n## Code Example\n```javascript\nasync function placeFraudAlert(reqId, payload){\n  // Redis: cache with short TTL\n  await redis.set(`alert:${reqId}`, JSON.stringify(payload), 'EX', 3600);\n\n  // MongoDB: embed alert reference in customer profile\n  await mongo.updateOne({customerId: payload.customerId},\n    { $push: { alerts: { id: reqId, ...payload } } }, { upsert: true });\n\n  // Cassandra: append per-customer transaction event\n  await cassandra.execute(`INSERT INTO alerts_by_customer (customer_id, ts, req_id, details) VALUES (?, toTimeuuid(now()), ?, ?)`,\n    [payload.customerId, reqId, payload]);\n\n  // DynamoDB: immutable audit log\n  await dynamo.putItem({\n    TableName: 'Audit',\n    Item: { PK: { S: `ALERT#${reqId}` },\n            ...payloadToDynamoItem(payload) }\n  });\n}\n```\n\n## Follow-up Questions\n- How would you test race conditions between stores during high load?\n- How would you handle historical backfills if Redis data was evicted earlier than TTL?",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-4",
        "questionId": "q-28570",
        "question": "Scenario: A food-delivery platform maintains restaurant menus in MongoDB, per-area live inventory in Redis (rest:<restaurantId>:area:<areaId> dishId->qty), orders in DynamoDB, and daily analytics in Cassandra. When a user places an order, reserve inventory in Redis and write an order to DynamoDB, validating menus in MongoDB and logging analytics in Cassandra. Design a cross-store commit protocol (Saga or 2PC), concrete schemas, idempotent writes, and a practical test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a normalized core with a generated column for device_type from payload, plus targeted indexes: a partial index on occurred_at for last 90 days and a covering index on (tenant_id, event_name, occur",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Redis: session:{user}:{device} = token (TTL 24h); active_devices:{user} add device. MongoDB: update users set last_login, push device to devices array. Cassandra: insert into login_events (user_id",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ride_id is cross-store source of truth. Redis: HSET station:{station_id}:bikes:{bike_id} 0 and a 60s TTL; publish cache-invalidation. MongoDB: upsert a ride document {ride_id, user_id, station_id, bik",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a cross-store order placement system using an **Ordered Saga** pattern with compensating transactions: 1. **Validate** menu items exist in MongoDB (read-only, no compensation needed) 2. **Reserve inventory** in R…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates your ability to coordinate transactions across four different datastores (Redis, MongoDB, DynamoDB, Cassandra) while handling compensation logic and idempotency.\n\n## Key Concepts\n\n| Pattern | Use Case | Trade-offs |\n|---------|----------|------------|\n| **Saga (Ordered)** | Sequential steps with compensating actions | No distributed lock; eventual consistency |\n| **2PC** | Not recommended across heterogeneous stores | Blocking, coordinator failure risks |\n| **Outbox + CDC** | Reliable async event propagation to Cassandra | Adds latency; eventual consistency |\n\n## Concrete Schemas\n\n**Redis**: `rest:{restaurantId}:area:{areaId}:dish:{dishId}` → `qty` (integer)\n\n**DynamoDB Orders**:\n- PK: `orderId` (ULID for sort key on userId)\n- Attributes: `userId`, `restaurantId`, `items[]`, `total`, `status`, `idempotencyKey`, `createdAt`\n\n**MongoDB Menu**: `{ restaurantId, items: [{ dishId, name, price, available }] }`\n\n**Cassandra Analytics**: `orders_by_day` table partitioned by `date`, aggregating order counts and revenue.\n\n## Idempotent Write Strategy\n\n```javascript\n// DynamoDB conditional write\nawait docClient.put({\n  TableName: 'Orders',\n  Item: order,\n  ConditionExpression: 'attribute_not_exists(idempotencyKey)'\n});\n\n// Redis reservation with idempotency set\nconst lockKey = `reserve:${orderId}:${dishId}`;\nawait redis.set(lockKey, '1', 'NX', 'EX', 3600);\n```\n\n## Test Plan\n\n| Test Case | Method | Expected Result |\n|-----------|--------|----------------|\n| Happy path order | E2E integration test | Inventory decremented, order created, analytics logged |\n| Double-submit | Send same idempotency key twice | Second request returns original order (idempotent) |\n| Redis reserve failure | Mock Redis DECRBY returning negative | Compensating INCRBY executed, order not written |\n| DynamoDB failure | Inject throttling | Saga retries with exponential backoff, eventual success |\n| Cassandra lag | Check analytics after 5s | Analytics appear eventually |\n| Concurrent orders for same dish | Race condition test | No negative inventory; both succeed or one fails gracefully |",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-5",
        "questionId": "q-24776",
        "question": "Given a PostgreSQL 14 database storing clickstream events in a table partitioned by month (range partitioning), optimize a query that computes per-user daily active minutes for a date range and joins a users table to fetch metadata. The current plan scans all partitions; propose a partitioning strategy, indexing plan, and how to ensure partition pruning and avoid cross-partition scans. Include schemas, analysis steps, and a practical test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "The physical plan demonstrates effective partition pruning on both date and region columns, with predicate pushdown directly into Delta Lake's storage layer. The small dimension table triggers a BroadcastHashJoin to avo…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "A fitness app records workouts across devices. Redis caches the latest 5 workouts per user with a 24h TTL, MongoDB stores user profiles, Cassandra holds time series workouts (user_id, ts, type, duration, calories) bucke…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Partition by range on event_time monthly and attach per-partition indexes on (user_id, event_type, event_time). Ensure the query includes event_time filters so pruning occurs; verify with EXPLAIN (ANA",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a unique event_id per operation. Redis: Lua script atomically increments user:score:24h and returns the new score. Cassandra: append a row (user_id, event_id, delta, ts). DynamoDB: update daily_su",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssess ability to design partitioned schemas that scale, reason about pruning, and justify index allocations and test plans in production-like workloads.\n\n## Key Concepts\n- Partition pruning (constraint exclusion) with range partitions\n- Range vs hash partitioning trade-offs for hot user segments\n- Per-partition indexes and why global indexes don’t exist on partitioned tables\n- Query design to enable pruning (filters on partition key)\n- Verification with EXPLAIN ANALYZE and BUFFERS\n\n## Code Example\n```sql\nCREATE TABLE events (\n  id bigserial PRIMARY KEY,\n  user_id bigint NOT NULL,\n  event_time timestamptz NOT NULL,\n  event_type text NOT NULL,\n  duration int NOT NULL\n) PARTITION BY RANGE (event_time);\n\nCREATE TABLE events_2024_01 PARTITION OF events FOR VALUES FROM ('2024-01-01') TO ('2024-02-01');\nCREATE TABLE events_2024_02 PARTITION OF events FOR VALUES FROM ('2024-02-01') TO ('2024-03-01');\n\nCREATE INDEX idx_events_2024_01 ON events_2024_01 (user_id, event_type, event_time);\nCREATE INDEX idx_events_2024_02 ON events_2024_02 (user_id, event_type, event_time);\n```\n\n```sql\nSELECT user_id, SUM(duration) AS active_minutes\nFROM events\nWHERE event_time >= '2024-01-01' AND event_time < '2024-02-01'\n  AND event_type = 'play'\nGROUP BY user_id;\n```\n\n## Follow-up Questions\n- How to handle growing partitions and ensure pruning remains effective as data scales?\n- What trade-offs exist if a small subset of users drives most queries, and how would you mitigate with subpartitioning or indexing changes?",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-6",
        "questionId": "q-643",
        "question": "How would you design an indexing strategy for a time-series database that handles both recent data queries and long-term historical analysis, considering the trade-offs between write performance and query efficiency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a tiered indexing strategy combining time-based partitioning with adaptive index types: recent data would utilize high-performance write-optimized indexes, while historical data would employ compressed…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use SERIALIZABLE isolation, lock both accounts with SELECT ... FOR UPDATE in a single transaction, check balance before debit, then credit and log transfer. To avoid deadlocks, always lock accounts in",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Normalize into contractors(id, region, skill, status) and assignments(id, contractor_id, start_date, end_date, pay_rate). Use a composite index on assignments (contractor_id, start_date, end_date) and",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a single SQL query that returns, for each customer, the most recent completed order per product category, using a window function partitioned by (customer_id, category_id) ordered by placed_at",
            "isCorrect": false
          }
        ],
        "explanation": "For a time-series database, I'd design a multi-tiered indexing approach that dynamically adapts to data age and access patterns. Recent data (last 30 days) would use a composite index on timestamp and primary key with minimal overhead structure, implementing LSM tree or skip list architectures to maximize write throughput while maintaining fast query performance. This tier prioritizes write efficiency since recent data represents a small but frequently accessed portion of the total dataset.\n\nFor medium-term data (1-12 months), I'd transition to more space-efficient indexing using compressed bitmap indexes or time-series specific structures that balance storage optimization with acceptable query performance. This tier would leverage data compression techniques and adaptive sampling to reduce storage footprint while maintaining reasonable analytical capabilities.\n\nFor historical data (12+ months), I'd implement highly compressed, columnar-based indexing optimized for analytical workloads, potentially utilizing data summarization and aggregation strategies to dramatically reduce storage requirements while supporting large-scale historical analysis queries.",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-7",
        "questionId": "q-14925",
        "question": "Scenario: An issue-tracking DB stores issue_id (PK), project_id, reporter_id, priority, status, created_at. You must support: (1) the latest open issues per project, and (2) lookups by issue_id. Propose concrete indexing: a composite B-tree index on (project_id, status, created_at DESC) for (1); a hash index on (issue_id) for (2). Include sample CREATE INDEX statements and explain why the order matters and maintenance trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a generated column amount_num numeric GENERATED ALWAYS AS ((payload->>'amount')::numeric) STORED and an index on (org_id, occurred_at::date, event_type) INCLUDE (account_id, amount_num). SQL: SELE",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "For a given user_id and last 30 days, filter plays, join songs and genres, group by genre, and pick top 5 by play_count with total_duration. Use a CTE to pre-filter plays, then join to songs and genre",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Composite B-tree idx on (project_id, status, created_at DESC) supports filtering by project, status, and sorting by recency in a single index. Hash idx on (issue_id) speeds exact lookups; note issue_i",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Run EXPLAIN ANALYZE to verify pruning; check that only relevant monthly partitions are scanned and that aggregation is driven by a date-based grouping. If pruning is absent, add a composite index on (",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nA practical test of index choice given common query patterns; distinguishes understanding of multi-column indexes and when hash indexes help.\n\n## Key Concepts\n- Composite indexes and leading column usage\n- B-tree vs Hash indexing trade-offs\n- Maintenance and scan patterns\n\n## Code Example\n```sql\nCREATE INDEX idx_issues_proj_status_created ON issues (project_id, status, created_at DESC);\nCREATE INDEX idx_issues_id_hash ON issues USING HASH (issue_id);\n```\n\n## Follow-up Questions\n- How would performance change if most queries filter by status only?\n- When would a covering index be preferred?",
        "difficulty": "beginner"
      },
      {
        "id": "database-q-8",
        "questionId": "q-15849",
        "question": "In PostgreSQL, design a funds-transfer operation that moves amount X from account A to B atomically under high concurrency. Outline a concrete write path: lock both rows (SELECT ... FOR UPDATE), validate balance, perform updates, and insert a ledger row, all in one transaction. Which isolation level and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use: A) compound B-tree index { userId:1, ts:-1 } to support latest 24h and per-user sorts. B) compound B-tree index { userId:1, eventType:1, ts:-1 } for the user+type hot path. C) hashed index on { e",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "To implement an atomic funds transfer from account A to account B in PostgreSQL under high concurrency, use a single transaction with row-level locking: 1. Begin transaction 2. Lock both account rows using SELECT ... FO…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Create a GiST index on the location column for spatial queries, a composite B-tree index on (price, bedrooms) to support price range and bedroom count filters, a partial index on (price, square_footage) WHERE is_active…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Composite indexes are optimal for queries that filter on multiple columns in a specific order, while single-column indexes work better for independent column queries.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical ACID reasoning and MVCC understanding in a real-world transactional context. It assesses the candidate's ability to design atomic operations under concurrent load and justify isolation level choices.\n\n## Key Concepts\n\n- **ACID properties**: Ensuring atomicity, consistency, isolation, and durability\n- **MVCC (Multi-Version Concurrency Control)**: How PostgreSQL manages concurrent transactions\n- **Row-level locking**: Using SELECT ... FOR UPDATE to serialize access\n- **Deadlock prevention**: Always acquiring locks in a consistent order (e.g., by account ID)\n- **Isolation level trade-offs**: READ COMMITTED vs SERIALIZABLE\n\n## Code Example\n\n```sql\nBEGIN TRANSACTION;\n\n-- Lock accounts in consistent order (lower ID first) to prevent deadlocks\nSELECT id, balance \nFROM accounts \nWHERE id IN (:a, :b) \nORDER BY id \nFOR UPDATE;\n\n-- Validate sufficient balance\nSELECT balance INTO :current_balance FROM accounts WHERE id = :a;\nIF :current_balance < :amount THEN\n    RAISE EXCEPTION 'Insufficient funds';\nEND IF;\n\n-- Perform transfer\nUPDATE accounts SET balance = balance - :amount WHERE id = :a;\nUPDATE accounts SET balance = balance + :amount WHERE id = :b;\n\n-- Record in ledger for audit trail\nINSERT INTO ledger (from_account, to_account, amount, created_at)\nVALUES (:a, :b, :amount, NOW());\n\nCOMMIT;\n```\n\n## Isolation Level Rationale\n\n**READ COMMITTED** is preferred over SERIALIZABLE because:\n- Row-level locks already serialize the critical section\n- The balance check within the transaction sees committed data\n- No phantom reads can occur on the locked rows\n- SERIALIZABLE would require retry logic due to serialization failures, adding complexity\n- Lower latency and better throughput under high concurrency",
        "difficulty": "beginner"
      },
      {
        "id": "database-q-9",
        "questionId": "q-9087",
        "question": "Schema: products(id, category, price, active); orders(id, region, created_at, status); order_items(id, order_id, product_id, quantity, unit_price); discounts(code, percent). For a given region and date range, return the top 5 categories by net revenue = sum(quantity * unit_price * (1 - percent)), only for active products and non-canceled orders. Show the exact JOINs, filtering, and aggregation, and propose concrete indexes to optimize the query?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: normalize campaigns by product, calculate revenue with discounts in a single query, filter to completed orders in range, group by category. Use a left join for campaigns to handle no-discoun",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Join orders→order_items→products, filter by region/date, exclude canceled orders and inactive products, and compute net revenue as sum(quantity*unit_price*(1-COALESCE(discount_percent,0))). Group by c",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use tenant_id as the leading column in all indexes, implement partial indexes for tenant-specific queries, and create separate summary tables with materialized views for cross-tenant reporting to bala",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use EXPLAIN ANALYZE to verify partition pruning for last 30 days on a range-partitioned events table. If pruning fails, check that the WHERE clause uses the partition key directly (no function on crea",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests multi-table joins, precise filtering, aggregation, and index reasoning in PostgreSQL. Requires handling normalization decisions and edge cases like NULL discounts.\n\n## Key Concepts\n- Correct multi-join order and WHERE filtering for region/date/status\n- Financial calculation with COALESCE and NULL handling\n- Composite and partial indexes to optimize group-by scans\n- Validation with EXPLAIN ANALYZE and realistic data distributions\n\n## Code Example\n```javascript\nSELECT p.category,\n       SUM(oi.quantity * oi.unit_price * (1 - COALESCE(d.percent, 0))) AS net_revenue\nFROM orders o\nJOIN order_items oi ON oi.order_id = o.id\nJOIN products p ON p.id = oi.product_id\nLEFT JOIN discounts d ON o.discount_code = d.code\nWHERE o.region = $1\n  AND o.created_at BETWEEN $2 AND $3\n  AND o.status <> 'canceled'\n  AND p.active = true\nGROUP BY p.category\nORDER BY net_revenue DESC\nLIMIT 5;\n```\n\n## Follow-up Questions\n- How would you handle ties in top categories?\n- How would you validate index effectiveness with real data volumes and skew?",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-10",
        "questionId": "q-10705",
        "question": "Scenario: A streaming app caches presence in Redis, stores profiles in MongoDB, daily activity in Cassandra, and profile history in DynamoDB. You add a required field timezone to profiles. Design concrete schemas and write paths: Redis key example, Mongo document, Cassandra table, DynamoDB item; TTLs; an idempotent multi-store write; a backfill plan for existing users (UTC default); and a simple query path to read current profile with timezone?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design composite and covering indexes for a mixed read-write analytics workload, balancing query performance against index maintenance overhead.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create a composite index on (customer_id, status, created_at) with columns in order of selectivity - most selective first - to allow the database to filter most effectively using the index.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a comprehensive timezone rollout across four data stores: Redis presence cache using key `user:presence:{id}` with 300s TTL storing `{userId, timezone, lastSeen}`; MongoDB profile document `{userId, name, timezon…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a Serializable transaction in PostgreSQL. Schema: accounts(id, balance), transfers(id, from_id, to_id, amount, txid, status). Pattern: BEGIN; SELECT balance FROM accounts WHERE id IN (from_id, to_",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests multi-store schema evolution and safe rollout capabilities across cache and databases—a critical real-world engineering challenge.\n\n## Key Concepts\n- Cross-store schema evolution with backward compatibility\n- Idempotent writes and retry safety via request deduplication\n- TTL management and cache coherence between stores\n- Backfill strategies with minimal downtime using batch processing\n- Read-through caching patterns for optimal performance\n\n## Code Example\n```javascript\n// Pseudo flow for timezone update across stores\nfunction applyTimezoneUpdate(userId, tz, requestId) {\n  // Idempotent write with deduplication\n```",
        "difficulty": "beginner"
      },
      {
        "id": "database-q-11",
        "questionId": "q-30150",
        "question": "Scenario: A PostgreSQL events table stores user_id (int), event_type (text), created_at (timestamptz). To support: (a) latest N events per user, (b) all events of a specific type in a date range. Propose a practical indexing strategy using a composite B-tree, a separate hash index, and minimal maintenance. Include index definitions and sample queries with EXPLAIN ANALYZE?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use SERIALIZABLE isolation to avoid anomalies; wrap transfer in a transaction with two rows FOR UPDATE; if from balance >= amount then debit and credit; commit; on 40001, rollback and retry with backo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a composite B-tree on (user_id, created_at DESC) to fetch a user’s latest events efficiently, plus a hash index on (event_type) for fast type filters. Examples:\\nCREATE INDEX events_user_created_a",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Run EXPLAIN ANALYZE to verify pruning; check that only relevant monthly partitions are scanned and that aggregation is driven by a date-based grouping. If pruning is absent, add a composite index on (",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Create a composite BTREE index on (user_id, created_at DESC) to efficiently fetch the latest 25 events per user. Additionally, create a composite BTREE index on (user_id, event_type, created_at DESC) to support counting…",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\\n- Tests basic indexing intuition for per-user ordering and global filtering\\nKey Concepts\\n- Composite B-tree, hash index, index scans, partial ordering\\nCode Example\\n```sql\\nCREATE INDEX events_user_created_at_idx ON events (user_id, created_at DESC);\\nCREATE INDEX events_type_hash_idx ON events USING hash (event_type);\\n```\\nFollow-up Questions\\n- What about bloat and maintenance if there are many updates?\\n- How would you adapt if created_at is non-unique per user?",
        "difficulty": "beginner"
      },
      {
        "id": "database-q-12",
        "questionId": "q-30070",
        "question": "A social app publishes a 'story' that must be visible in Redis feeds in real time, stored canonically in MongoDB, appended to Cassandra activity logs, and archived in DynamoDB with TTL. Design the cross-store write path, key schemas, TTL strategies, and conflict resolution. Explain read paths for fresh vs archived stories and compare 2PC vs saga trade-offs with concrete examples?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a saga-based write path with the following cross-store operations: First, publish story_id to Redis feeds using the key feeds:stories:{user_id} as a sorted set with timestamp scores for real-time feed delivery. N…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Check partition pruning effectiveness, index utilization patterns, and expensive sort operations. Create composite indexes on (date, filtered_columns) and evaluate clustering strategies for optimal data access.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would implement a multi-layered indexing strategy that balances query performance with write overhead. Start with a composite index on the most selective attributes that are commonly used together—typically (category,…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "**Architecture Overview** **Redis (Hot Feature Cache)** - Key: `feat:{userId}:vector` (float32 array, 128–512 dimensions) - TTL: 5 minutes - Write-back policy: Asynchronous write on feature update via Kafka consumer; ba…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates your ability to design distributed writes across multiple stores with different consistency and latency profiles. It requires understanding pragmatic patterns like Saga over traditional 2PC, explicit failure handling strategies, and read-time trade-offs between fresh and archived data.\n\n## Key Concepts\n- Saga vs 2PC for cross-store commits\n- Idempotency and deduplication across stores\n- TTL and eviction semantics in Redis and DynamoDB\n- Read path prioritization: fresh (Redis/MongoDB) vs archived (DynamoDB) data\n- Compensation logic and eventual consistency",
        "difficulty": "advanced"
      },
      {
        "id": "database-q-13",
        "questionId": "q-6835",
        "question": "Design an indexing strategy for a high-throughput user activity table with columns: user_id, event_type, event_ts, region, device_id, payload. The system must support: 1) latest 5 events per user, 2) counts by (region, event_type) in 1-hour windows, 3) fast lookups by (event_type, region, event_ts) for recent aggregates. Propose a mix of B-tree indexes (for ranges), a hash index (for some equality lookups), and composite indexes; specify key orders, TTL/partitioning strategies, and how to avoid hot spots?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed: 1) B-tree composite index on (user_id, event_ts DESC) to fetch latest events quickly. 2) Hash index on (region, event_type) for fast exact counts. 3) Composite index on (event_type, region,",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Schema: tenants(id), users(id, tenant_id), events(id, user_id, tenant_id, event_type, created_at, payload). For a tenant and date window, return per-user counts by event_type. Optimized query: SELECT",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Compute risk in Redis with a Lua script that uses tx_id, amount, and user history, then writes risk:tx:<id> with score and ts. Use DynamoDB TransactWriteItems to write region-scoped score and a summar",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Find posts in the date window that have the tag via joins to post_tags and tags, count the posts, sum their comments, and compute per-user post counts to average them. Use left join for comments, subq",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests precise index design across B-tree, hash, and composite indexes for real-world query patterns with order-by, time windows, and aggregations. Requires trade-offs between write amplification, storage, and read latency.\n\n## Key Concepts\n- B-tree indexing for range and order by on time-series data\n- Hash indexing for equality-lookup hot paths\n- Composite index key order matters for selectivity and query shape\n- Partial indexes to limit maintenance to recent data\n- Hot-spot avoidance via bucketing or partitioning\n\n## Code Example\n```\nCREATE INDEX idx_user_eventts ON user_events (user_id, event_ts DESC);\nCREATE INDEX idx_region_eventtype_hash ON user_events USING HASH (region, event_type);\nCREATE INDEX idx_evtype_region_eventts ON user_events (event_type, region, event_ts DESC);\n```\n\n## Follow-up Questions\n- How would you verify index effectiveness with EXPLAIN plans and real workload metrics?\n- How would you adapt this strategy if regional skew causes hot partitions?",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-14",
        "questionId": "q-13227",
        "question": "In a collaborative document app using Redis for live cursors, MongoDB for the latest document, Cassandra for edit history, and DynamoDB for archival snapshots, design a concrete multi-store write for an edit. Include concrete key schemas, TTLs, idempotence, cross-store invalidation, and a read path to fetch current content plus live cursors and recent history?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "READ COMMITTED path: BEGIN; SELECT balance FROM accounts WHERE id IN ('A','B') FOR UPDATE; if A>=100 then UPDATE accounts SET balance=balance-100 WHERE id='A'; UPDATE accounts SET balance=balance+100",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a multi-store write for an edit as follows: MongoDB stores the latest document {docId, version, content, updatedAt}; Redis stores live cursors as {docId:userId} with TTL; Cassandra appends edit events {docId,…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use composite indexes with high-cardinality columns first (customer_id), include frequently accessed columns for covering indexes, and consider partial indexes for common query patterns. Avoid indexing low-cardinality c…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Create a composite index on (customer_id, status, created_at DESC) for query #1, a BRIN index on created_at for time-range queries, and a partial index for pending orders. Balance index cardinality, selectivity, and wri…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests multi-store write coordination, idempotence, and read-path composition across Redis, MongoDB, Cassandra, and DynamoDB in a realistic collaboration app.\n\n## Key Concepts\n- Cross-store writes and eventual consistency\n- TTL-driven lifecycle for cursors and snapshots\n- Idempotence via writeId; conflict resolution with versioning\n- Read path assembly from multiple stores\n\n## Code Example\n```javascript\n// Example schemas (illustrative)\nconst doc = { docId, version, content, updatedAt }\nconst cursors = { docId, userId, position }\nconst events = { docId, version, op, timestamp, writeId }\nconst snapshots = { docId, snapshotVersion, content, createdAt }\n\n// Multi-store write implementation\nasync function writeEdit(docId, edit, writeId) {\n  // 1. Update MongoDB with version check\n  const currentDoc = await MongoDB.findOne({docId})\n  if (currentDoc.version !== edit.expectedVersion) {\n    throw new Error('Version conflict')\n  }\n  \n  // 2. Atomic cross-store operations\n  await Promise.all([\n    MongoDB.updateOne(\n      {docId}, \n      {$set: {content: edit.content, version: edit.version, updatedAt: new Date()}}\n    ),\n    Cassandra.insert({\n      docId, \n      version: edit.version, \n      operation: edit.operation, \n      timestamp: new Date(),\n      writeId\n    }),\n    Redis.del(`doc:${docId}:cursors`) // Invalidate cursor cache\n  ])\n  \n  // 3. Periodic snapshot to DynamoDB (async)\n  if (edit.version % 10 === 0) {\n    await DynamoDB.put({\n      docId,\n      snapshotVersion: edit.version,\n      content: edit.content,\n      createdAt: new Date()\n    })\n  }\n}\n\n// Read path composition\nasync function readDocument(docId) {\n  const [doc, cursors, recentEvents] = await Promise.all([\n    MongoDB.findOne({docId}),\n    Redis.hgetall(`doc:${docId}:cursors`),\n    Cassandra.find({docId}).sort({timestamp: -1}).limit(50)\n  ])\n  \n  return { doc, cursors, recentEvents }\n}\n```\n\n## Implementation Notes\n- Use writeId for idempotence across all stores\n- Implement exponential backoff for cross-store conflicts\n- Consider read-repair for eventual consistency scenarios",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-15",
        "questionId": "q-8960",
        "question": "Scenario: An e-commerce checkout uses a stock table (product_id, quantity, version). Two concurrent checkouts may reserve the last unit. Design an MVCC-based reservation protocol: choose an isolation level, implement per-row versioning, and provide concrete SQL for an atomic reserve with a version check. How would you test for write skew and phantom reads?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a SERIALIZABLE transaction in Postgres to debit and credit, and write a corresponding event into an outbox table within the same transaction. A separate worker streams the outbox to Kafka with a t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a hybrid index: cluster the main table by (user_id, ts) to optimize per-user time-window scans; maintain a hash index on (user_id, event_type) for fast counts; and a composite index on (user_id, a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Partition the events table by RANGE on event_time using monthly partitions. For each partition, create a covering index that includes both the partition key and the filtering/aggregation columns: `CREATE INDEX ON events…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "SERIALIZABLE isolation; per-row versioning via a version column. Atomic reserve: BEGIN; SET TRANSACTION ISOLATION LEVEL SERIALIZABLE; UPDATE stock SET quantity = quantity - 1, version = version + 1 WH",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nRealistic ACID behavior in high-contention checkout flows; MVCC with explicit versioning shows practical locking vs optimistic updates.\n\n## Key Concepts\n- SERIALIZABLE vs snapshot isolation; serialization_failure retries\n- MVCC, per-row versioning, optimistic locking\n- Atomic updates and idempotent retries\n\n## Code Example\n```sql\nBEGIN;\nSET TRANSACTION ISOLATION LEVEL SERIALIZABLE;\nUPDATE stock SET quantity = quantity - 1, version = version + 1\nWHERE product_id = $1 AND quantity > 0 AND version = $2 RETURNING 1;\nCOMMIT;\n```\n\n## Follow-up Questions\n- How would you implement a robust retry policy for serialization failures?\n- How would you extend this to distributed inventory across services?",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-16",
        "questionId": "q-19324",
        "question": "You have a table events(event_id BIGINT PRIMARY KEY, user_id INT, event_type VARCHAR(16), created_at TIMESTAMPTZ, data JSONB). The workload is beginner-friendly: fetch the latest 100 events for a user in a date window; count events by user and type within a date window; and retrieve the most recent event per user. Design a concrete indexing strategy using B-tree, hash, and composite indexes (and optional partial indexes). Include concrete CREATE INDEX statements and explain tradeoffs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "SQL plan (category and top products): SELECT pr.category AS category, SUM(oi.quantity*oi.unit_price) AS revenue FROM orders ord JOIN order_items oi ON oi.order_id = ord.id JOIN products pr ON oi.produ",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create a GiST index on the location column for spatial queries, a composite B-tree index on (price, bedrooms) to support price range and bedroom count filters, a partial index on (price, square_footage) WHERE is_active…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "For the events table with the given workload, here is a concrete indexing strategy: -- Primary key is already indexed (B-tree by default) -- 1. Composite B-tree for fetching latest 100 events per user in date range CREA…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a single multi‑document transaction with readConcern: { level: 'snapshot' } and writeConcern: { w: 'majority' }. Read quotas.featureA, verify >= amount, then quotas.featureA -= amount and write an",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses practical indexing decisions for common read-heavy patterns, illustrating trade-offs between B-tree, hash, and composite indexes, plus the value of partial indexes for recent data.\n\n## Key Concepts\n- Composite B-tree indexes support multi-column range queries efficiently when columns are used in ORDER BY\n- Hash indexes work well for equality filters but don't support range queries\n- Partial indexes reduce storage and maintenance overhead by indexing only relevant data\n- Column order in composite indexes matters: equality conditions first, then range/sort columns\n\n## Trade-offs\n- Write amplification: more indexes slow down INSERT/UPDATE operations\n- Storage overhead: each index consumes additional disk space\n- Maintenance: indexes must be maintained via VACUUM to stay efficient\n- Partial indexes require careful predicate selection—too narrow limits usefulness, too wide defeats the purpose",
        "difficulty": "beginner"
      },
      {
        "id": "database-q-17",
        "questionId": "q-365",
        "question": "You're designing a real-time analytics system for Discord that processes millions of message events per minute. Your PostgreSQL database is experiencing severe write contention on the message_events table. How would you design a partitioning strategy using declarative partitioning, and what specific index optimizations would you implement to handle both time-series queries and user-based lookups efficiently?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Best plan uses the composite index on (event_type, event_time): equality on event_type plus the time-range; enables index scan with ordering by event_time. The hash index on event_type is not helpful",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a SERIALIZABLE transaction to debit A and credit B in a single commit, locking both rows with SELECT FOR UPDATE and writing to a central ledger. Validate balance before debit; ensure idempotence b",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement declarative partitioning with time-based partitions (hourly for high-traffic periods, daily for standard load) on the message_events table. Create BRIN indexes on timestamp columns for efficient range queries,…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a comprehensive indexing strategy using composite B-tree indexes on frequently filtered columns (category_id, price_range, customer_rating), partial indexes for active/inactive products, and scheduled index ma…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nDiscord processes millions of message events per minute, making database write contention a critical scalability challenge. This question assesses understanding of PostgreSQL's advanced partitioning capabilities and index optimization strategies for high-throughput, time-series workloads.\n\n## Expected Answer\nStrong candidates will discuss:\n- Declarative partitioning strategy (hourly partitions during peak load, daily partitions for normal periods)\n- BRIN indexes on timestamp columns for space-efficient range scans\n- B-tree indexes on user_id for rapid user-specific queries\n- Composite (user_id, timestamp) indexes for common analytical query patterns\n- Partition pruning and constraint exclusion optimization\n- Automated partition creation and maintenance workflows\n\n## Code Example\n```sql\nCREATE TABLE message_events (\n    id BIGSERIAL,\n    user_id BIGINT NOT NULL,\n    event_type VARCHAR(50),\n    payload JSONB,\n    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()\n) PARTITION BY RANGE (created_at);\n\n-- Create hourly partitions for recent data\nCREATE TABLE message_events_2024_01_01_h00 PARTITION OF message_events\n    FOR VALUES FROM ('2024-01-01 00:00:00') TO ('2024-01-01 01:00:00');\n\n-- Indexes\nCREATE INDEX CONCURRENTLY idx_message_events_user_id ON message_events (user_id);\nCREATE INDEX CONCURRENTLY idx_message_events_timestamp_brin ON message_events USING BRIN (created_at);\nCREATE INDEX CONCURRENTLY idx_message_events_user_time ON message_events (user_id, created_at);\n```",
        "difficulty": "advanced"
      },
      {
        "id": "database-q-18",
        "questionId": "q-26611",
        "question": "In PostgreSQL, a fact table sales_fact is partitioned by range on sale_date into monthly partitions (YYYY-MM). A dimension table regions(region_id, name). Build an optimized query to return total revenue per region for the trailing 12 months. Explain how to check partition pruning via EXPLAIN ANALYZE and how the plan would look with a filter on sale_date. Describe how to adjust partitioning to avoid over-scanning, and what index strategies support this. Provide schemas, a concrete query, and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Prune partitions with a trailing 12-month sale_date filter, join to regions, and aggregate by region. Put an index on (region_id, sale_date) in each partition and rely on constraint exclusion. Verify",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Create a composite BTREE index on (user_id, created_at DESC) to efficiently fetch the latest 25 events per user. Additionally, create a composite BTREE index on (user_id, event_type, created_at DESC) to support counting…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Proposed: composite B-tree index on (region, signup_year) INCLUDE (user_id, last_login, status) to cover range scans and point lookups. Skip the separate hash index on user_id since B-tree already handles point lookups…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use EXPLAIN ANALYZE to confirm partition pruning on event_date; rewrite filters to use a direct date range (no function) so only relevant partitions are read; enable constraint_exclusion if needed; co",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical understanding of partition pruning, explain plans, and validation in real workloads.\n\n## Key Concepts\n\n- Partition pruning via constraint exclusion\n- EXPLAIN ANALYZE plan shapes (Append, Index Scan)\n- Multi-column indexes on partitioned tables\n\n## Code Example\n\n```sql\n-- example schemas\nCREATE TABLE regions (\n  region_id int PRIMARY KEY,\n  name text\n);\n\nCREATE TABLE sales_fact (\n  sale_id bigserial PRIMARY KEY,\n  region_id int REFERENCES regions(region_id),\n  amount decimal(12,2),\n  sale_date date\n) PARTITION BY RANGE (sale_date);\n\n-- partitions for 12 months would be created with CHECK constraints on sale_date\n```\n\n## Follow-up Questions\n\n- How would you handle late data or partition maintenance for new months?\n- How would you measure pruning and detect non-pruning in production plans?",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-19",
        "questionId": "q-14057",
        "question": "Scenario: An e-commerce platform has a products table with 10M rows, frequently searches by category + price range, and filters by brand. The query `SELECT * FROM products WHERE category = 'electronics' AND price BETWEEN 100 AND 500 AND brand = 'Samsung'` is slow. Design an optimal indexing strategy including composite indexes, covering indexes, and index maintenance. Explain how you would handle index fragmentation and when to use partial indexes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create a composite index on (category, price, brand), implement covering indexes for frequently accessed columns, use partial indexes for active products, and maintain index health through regular fragmentation analysis…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a single SQL with two CTEs: filter paid orders in the month for city via joins; second aggregate per seller_id computing revenue, orders, unique_buyers, refunds; apply COALESCE. Index strategy: pa",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a hybrid plan: 1) B-tree composite index on (region, device_type, event_time DESC) to support range queries; 2) hash index on (user_id, event_id) for exact lookups; 3) composite index on (regi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use an explicit transfer transaction with row-level locking to prevent race conditions. Example: BEGIN; SELECT balance FROM accounts WHERE id=$src FOR UPDATE; SELECT balance FROM accounts WHERE id=$dst FOR UPDATE; IF ba…",
            "isCorrect": false
          }
        ],
        "explanation": "For this e-commerce scenario, I would design a comprehensive indexing strategy starting with a composite index on (category, price, brand) in that specific order since category typically has high cardinality and price range filtering provides the most selective operation. This index can satisfy the WHERE clause without requiring table lookups. Additionally, I would create a covering index including frequently selected columns like product_name, stock_quantity, and rating to eliminate key lookups entirely: `CREATE INDEX idx_products_search ON products(category, price, brand) INCLUDE (product_name, stock_quantity, rating)`. For partial indexes, I would create indexes on active products only (`WHERE status = 'active' AND stock_quantity > 0`) to reduce index size and improve maintenance efficiency. Index fragmentation should be monitored regularly using system functions, with rebuilding triggered when fragmentation exceeds 30% for large tables or 20% for frequently accessed ones. This strategy ensures optimal query performance while minimizing storage overhead and maintenance costs.",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-20",
        "questionId": "q-17336",
        "question": "Scenario: A real-time chat app stores presence in Redis (presence:{user_id} -> room_id, ttl 60s), messages in MongoDB (messages:{room_id}:{ts}, {sender, text}), daily metrics in Cassandra, and user profiles in DynamoDB. When a user sends a message to a room, design a concrete write path with keys, TTLs, and cross-store invalidation. Also describe how to read the latest 20 messages for that room?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Redis: presence:{user_id} = room_id with ttl 60s; MongoDB: insert into messages:{room_id} with {ts, sender, text} and index (room_id, ts); Cassandra: upsert daily_stats(room_id, date) += 1; DynamoDB:",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Create a composite index for the most common query patterns, include selective columns first, use covering indexes to avoid lookups, and balance read performance with write overhead by monitoring inde",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Two-level partitioning: range on event_date with region subpartitions; ensure constraint exclusion. Create a covering index on (region, user_id) per leaf; rewrite the query to restrict the date and re",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a partial index on (customer_id, created_at) filtering on status='pending', and create a supporting index on customers(tier) for the subquery. Validate with EXPLAIN ANALYZE to confirm index-only scans are being used…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests multi-store write paths, TTL usage, and cross-store invalidation in a beginner-friendly setting, plus a concrete read path for recent messages.\n\n## Key Concepts\n- Cross-store write patterns across Redis, MongoDB, Cassandra, DynamoDB\n- TTL management for presence data\n- Indexing strategy for fast reads of latest messages\n- Idempotency and simple deduplication keys\n\n## Code Example\n```javascript\n// Pseudo write path across stores\nconst ts = Date.now();\nredis.setEx(`presence:${userId}`, 60, roomId);\ndb.mongo.messages.insert({ room_id: roomId, ts, sender: userId, text });\ndb.cassandra.daily_stats.upsert({ room_id: roomId, date: new Date().toDateString(), +1 });\ndb.dynamo.update({ TableName: 'Users', Key: { id: userId }, UpdateExpression: 'ADD message_count :inc', ExpressionAttributeValues: { ':inc': 1 } });\n```\n\n## Follow-up Questions\n- How handle duplicates on retry?\n- How would you adjust TTLs for presence under burst activity?",
        "difficulty": "beginner"
      },
      {
        "id": "database-q-21",
        "questionId": "q-9071",
        "question": "Scenario: In PostgreSQL, two concurrent transactions try to transfer 100 from A to B and 100 from B to A. A starts with 150, B with 50. Explain how MVCC and isolation levels handle this to guarantee ACID. Provide concrete SQL paths used to avoid overdraft: (a) READ COMMITTED with row locking, (b) SERIALIZABLE with retries, and note potential deadlocks or retries. Which approach yields predictable results and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a composite B-tree index on (user_id, created_at DESC) for primary feed queries, complemented by a separate index on engagement_score for trending posts retrieval. For exact user_id lookups, consider a hash index fo…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "- Use a single ACID transaction that debits A and credits B atomically. - Represent money in integer cents, and lock rows with FOR UPDATE to avoid race conditions. - Example (PostgreSQL): ``` BEGIN; UPDATE accounts SET…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "READ COMMITTED path: BEGIN; SELECT balance FROM accounts WHERE id IN ('A','B') FOR UPDATE; if A>=100 then UPDATE accounts SET balance=balance-100 WHERE id='A'; UPDATE accounts SET balance=balance+100",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement Add-To-Cart with Redis for UI, Cassandra audit, MongoDB snapshot on checkout. Redis key cart:<userId> as hash of itemId->qty, TTL 1 day. On add, HINCRBY; Cassandra insert daily_cart_audit(us",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of how MVCC and isolation levels interact in concurrent transactions.\n\n## Key Concepts\n\n- MVCC and versioned reads\n- Read Committed vs Serializable\n- FOR UPDATE row locking\n- Deadlocks and retry semantics\n\n## Code Example\n\n```sql\nBEGIN;\nSELECT balance FROM accounts WHERE id IN ('A','B') FOR UPDATE;\n-- validation\nUPDATE accounts SET balance = balance - 100 WHERE id='A';\nUPDATE accounts SET balance = balance + 100 WHERE id='B';\nCOMMIT;\n```\n\n## Follow-up Questions\n\n- How would results differ under Retryable vs non-Retryable failures?\n- How would you extend to multiple accounts and transfers?",
        "difficulty": "beginner"
      },
      {
        "id": "database-q-22",
        "questionId": "q-16609",
        "question": "A lightweight social app uses Redis for sessions, MongoDB for profiles, Cassandra for time-series activity, and DynamoDB for per-user feature flags. When a user logs in on a new device, design a concrete write path with explicit keys and TTLs to create a session, log the event, and update the profile; also outline a read path to validate the session and fetch current profile and latest login activity, considering eventual consistency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a partial index on (customer_id, created_at) filtering on status='pending', and create a supporting index on customers(tier) for the subquery. Validate with EXPLAIN ANALYZE to confirm index-only scans are being used…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Redis: session:{user}:{device} = token (TTL 24h); active_devices:{user} add device. MongoDB: update users set last_login, push device to devices array. Cassandra: insert into login_events (user_id",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a Redis-based advisory lock (SETNX with TTL) to gate a stock decrement in MongoDB, then append an event to Cassandra and a summary entry in DynamoDB, all guarded by idempotent writes and a compens",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a normalized schema with users, teams, memberships, projects, and tasks as separate 3NF tables. For a team_id and month, join tasks to projects to filter by team, join memberships to restrict t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of basic cross-datastore write flows, TTL usage, and idempotent cache updates in a beginner context.\n\n## Key Concepts\n- TTL-driven cache invalidation across Redis\n- Cross-store writes: Redis, MongoDB, Cassandra, DynamoDB\n- Eventual consistency and read-path fallbacks\n\n## Code Example\n```javascript\n// Pseudo write flow\nredis.set(\"session:\"+user+\":\"+device, token, 24*3600);\ndbMongo.users.updateOne({_id: user}, {$set: {last_login: now}, $push: {devices: device}});\ncassandra.login_events.insert({user_id: user, ts: now, device: device}, TTL: 7*24*3600);\ndynamo.upsert({TableName: \"UserFlags\", Item: {user_id: user, flags: flags}});\n```\n\n## Follow-up Questions\n- How would you test race conditions when login occurs from multiple devices?\n- How would you adjust TTLs and invalidation strategies for latency across regions?",
        "difficulty": "beginner"
      },
      {
        "id": "database-q-23",
        "questionId": "q-15250",
        "question": "Scenario: A mobile fitness app collects live workout data. Redis stores per-session state (session:{id}:state, session:{id}:hr), MongoDB holds user profiles, and Cassandra records per-workout time-series (session_id, ts, heart_rate, calories) with bucketing. DynamoDB stores weekly aggregates. For a new workout payload (userId, sessionId, workoutType, startTs, initialHR), design concrete write paths, TTLs, and cross-store invalidation. Then outline a read path to fetch current HR, last 3 workouts, and weekly calories, with consistency notes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use partial indexes for high-selectivity filters to reduce storage footprint, order composite indexes with the highest-cardinality column first (tenant_id), and create covering indexes to eliminate heap fetches. Balance…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a composite PostgreSQL index on (category, price, rating DESC) with event-driven Elasticsearch synchronization via CDC, and utilize Elasticsearch's nested boolean queries for optimal multi-criteria filtering p…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Filter on the partition key directly and avoid applying functions to event_month. Example: SELECT ... FROM events WHERE event_month BETWEEN '2024-01' AND '2024-03' AND region IN ('NA','EU'); In EXPLAIN PLAN, verify that…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Write path: Redis: HSET session:{id}:state in_progress; HSET session:{id}:hr {startHR}; EXPIRE 1h; MongoDB: upsert into sessions by {userId,sessionId} with workoutType and startTs; Cassandra: insert t",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests multi‑store data modeling and practical write/read paths across Redis, MongoDB, Cassandra, and DynamoDB. Emphasizes TTL strategy in Redis, bucketing in Cassandra, upserts in MongoDB, and incremental updates in DynamoDB.\n\n## Key Concepts\n- Multi‑store write paths with concrete keys and TTLs\n- Cache invalidation and eventual consistency across stores\n- Time-series bucketing and weekly aggregates\n\n## Code Example\n```javascript\n// Redis example (pseudo)\nHSET session:abc123 state in_progress\nHSET session:abc123 hr 72\nEXPIRE session:abc123 3600\n```\n\n## Follow-up Questions\n- How would you handle out-of-order data arrivals?\n- How would you roll back or idempotently apply repeated payloads?",
        "difficulty": "beginner"
      },
      {
        "id": "database-q-24",
        "questionId": "q-17202",
        "question": "In Delta Lake, table 'events' is partitioned by day on event_date with billions of rows. Write an EXPLAIN ANALYZE plan for a query filtering 2026-01-01 to 2026-01-31 and a large user_id IN list, and explain how partition pruning will work. Propose a pruning-friendly predicate and show how to verify pruning in the plan, including dynamic pruning considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "EXPLAIN ANALYZE should show partition pruning on event_date, with min/max stats used to prune partitions. Use predicates on the partition key, not wrapped in functions: event_date >= '2026-01-01' AND",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Write path: Cassandra: insert (user_id, product_id, ts); Redis: LPUSH user:recent:<uid> <product_id>, LTRIM 10, EXPIRE 7d; DynamoDB: increment daily_views by (date, product_id). Read path: Redis cache",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "MongoDB stores flexible JSON-like documents allowing varying schemas per user, ideal for profiles with optional fields. SQL uses fixed table rows with predefined columns, ensuring consistency but requiring schema migrat…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a comprehensive timezone rollout across four data stores: Redis presence cache using key `user:presence:{id}` with 300s TTL storing `{userId, timezone, lastSeen}`; MongoDB profile document `{userId, name, timezon…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical understanding of partition pruning and EXPLAIN ANALYZE in Delta Lake.\n\n## Key Concepts\n- Partition pruning\n- Predicate pushdown\n- Delta statistics\n- Dynamic pruning\n- EXPLAIN ANALYZE\n\n## Code Example\n```sql\nEXPLAIN ANALYZE\nSELECT *\nFROM events\nWHERE event_date >= '2026-01-01' AND event_date < '2026-02-01'\n  AND user_id IN (...);\n```\n\n## Follow-up Questions\n- How does dynamic pruning affect this plan and what settings control it?\n- How would Z-Order clustering interact with partition pruning here?",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-05-02T08:13:27.559Z",
    "version": 1
  }
]